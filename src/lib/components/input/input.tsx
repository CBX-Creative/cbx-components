import * as React from 'react';import '@/index.css';import { cn } from '@/lib/utils';export type InputProps = React.InputHTMLAttributes<HTMLInputElement> &	React.TextareaHTMLAttributes<HTMLTextAreaElement> & {		type?: 'single-line' | 'multi-line';		className?: string;	};const SingleInput: React.FC<React.InputHTMLAttributes<HTMLInputElement>> = ({	className,	name,	value,	placeholder,	onChange,}) => {	return (		<input			className={cn(`flex h-[58px] w-full rounded-xl`, className)}			name={name}			value={value || ''}			placeholder={placeholder || ''}			onChange={onChange}		/>	);};const MultiInput: React.FC<	React.TextareaHTMLAttributes<HTMLTextAreaElement>> = ({ className, name, value, placeholder, onChange }) => {	return (		<textarea			className={cn(`flex h-[109px] w-full rounded-xl`, className)}			name={name}			value={value}			placeholder={placeholder}			onChange={onChange}		/>	);};export const CBXInput: React.FC<InputProps> = ({	type = 'single-line',	className,	name,	value,	placeholder,	onChange,}) => {	const El = type === 'single-line' ? SingleInput : MultiInput;	return (		<El			name={name}			value={value}			placeholder={placeholder}			onChange={onChange}			className={className}		/>	);};